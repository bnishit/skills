interface:
  display_name: "OpenRouter Integration"
  short_description: "OpenRouter discounts, models, media, routing, and costs"
  default_prompt: "Use $openrouter-integration to choose live models or discounted endpoints, add the right text or media API, and make routing, reasoning, spend, and fallback behavior production-safe."
